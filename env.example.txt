# Text-to-SQL Environment Variables
# Copy this file to .env and fill in your values
# Never commit .env to GitHub!

# ── AI Backend ─────────────────────────────────────────────────────────────────
# Options: ollama (default, offline), openai, anthropic
LLM_BACKEND=ollama

# Ollama model to use (must be pulled first: ollama pull llama3.2)
OLLAMA_MODEL=llama3.2

# OpenAI API key (only needed if LLM_BACKEND=openai)
# OPENAI_API_KEY=sk-...

# Anthropic API key (only needed if LLM_BACKEND=anthropic)
# ANTHROPIC_API_KEY=sk-ant-...

# ── Database ────────────────────────────────────────────────────────────────────
# SQLite path (default app)
DB_PATH=data/sample.db

# SQL Server settings (for agent.py)
# Replace with your own server name
SQL_SERVER=localhost\SQLEXPRESS
SQL_DATABASE=YouTubeAnalytics
